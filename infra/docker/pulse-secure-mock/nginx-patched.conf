events {}

http {
    server {
        listen 80;
        server_name _;

        location /dana-na/auth/url_default/welcome.cgi {
            return 200 '<html><body>Welcome to the Pulse Secure</body></html>';
            add_header Content-Type text/html;
        }

        # Patched version: any traversal attempt → 404
        location ~* "dana-na.*\.\." {
            return 404;
        }

        location / {
            return 404;
        }
    }
}
